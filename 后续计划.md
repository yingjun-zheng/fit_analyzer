# 骑行FIT数据分析器 · 后续开发计划

> 项目：`C:\Users\zhengyingjun\Documents\deepseek\fit_analyzer`
> 更新日期：2026-09-02
> 说明：本计划基于当前已完成的「复盘 Agent + 路书功能 + 训练负荷可视化 + 路径规划」整理，按优先级分阶段，供后续迭代参考。

---

## 一、已完成的能力（基线）

| 模块 | 说明 |
|------|------|
| FIT 解析 | fitparse，兼容缺字段/缺心率/本地时区，支持多品牌码表 |
| 复盘 Agent | 6 种意图路由（单次/周期/对比/训练负荷/体能），CLI + GUI 双入口 |
| 对比分析 | 同路线识别 + 逐项差值 + 进步趋势 |
| 训练负荷 | TSS/NP/IF + hrTSS 降级 + CTL/ATL/TSB（按日历天衰减） |
| 体能分析 | 心速比 / 有氧解耦 / 心率区间 / 踏频质量 |
| 训练负荷可视化 | 月度视图 CTL/ATL/TSB 三线图（全量数据按日历天连续衰减） |
| 路书功能 | 历史活动转路书 + GPX 导入 + 海拔剖面爬坡高亮（Cat4~HC）+ AI 解读难度 + 导出 + 海拔补全 |
| 路径规划 | 高德地图交互选点（行者风格）+ 途经点串联 + GCJ-02→WGS-84 坐标纠偏，可转路书分析 |
| 打包交付 | pack.py 一键打包（含 WebEngine 高德地图模式） |

---

## 二、后续计划（按优先级）

> 参考了 `hanfengown/fit-ride-analytics`（纯前端 FIT 分析 SPA）的思路，以下标 🧩 的为可借鉴功能点（仅借鉴算法口径/思路，不抄其 AGPL 代码）。

### ✅ 阶段一（已完成）：补强「训练教练」价值

1. **训练负荷趋势可视化** ✅ 已交付
   - 月度视图加 CTL/ATL/TSB 三指标曲线图
   - 价值：让「体能/疲劳/状态」变化肉眼可见，是训练教练的核心
   - 🧩 参考其「周训练负荷 wTSS」「TSS 趋势图」的呈现思路，可加一个按周汇总的负荷柱状/趋势维度

2. **心率区间深度总结**（未做）
   - 有氧区间（Z2）与阈值区间（Z4）的时间占比、配速对比
   - 价值：你的数据核心是心率（无功率计），这是最贴合实际的教练指标
   - 🧩 直接复用其「心率 5 区」切分口径：按最大心率 `50/60/70/80/90/100%` 分界，输出每区时长 + 时间占比明细（饼图/表格）
   - 🧩 若有功率数据，附带「功率 7 区」（按 FTP `0/55/75/90/105/120/150%`）同款明细

3. **FTP 自动估算**（低优先级，已搁置）
   - 无功率计情况下 FTP 意义有限，已判断价值低暂时搁置
   - 🧩 如需保留功率维度，可参考其三级回退：设备值 → 手动设置 → 20 分钟最佳功率 × 0.95；另可用「功重比 W/kg」替代绝对值，比 FTP 更通用

### ✅ 阶段二（已完成）：路书功能增强

1. **海拔剖面图高亮爬坡段** ✅ 已交付
   - 在海拔曲线上用色带标出 Cat4~HC 各爬坡段
   - 价值：路书难度一目了然
   - 🧩 参考其爬升去噪口径：缺 `total_ascent` 时按相邻海拔正增量累加，仅 `>0.5m` 计入，防 GPS 抖动灌水

2. **AI 解读路书难度** ✅ 已交付
   - 复用复盘 Agent 的 LLM，输入路书结构化数据（里程/爬升/坡级）→ 输出「这段路有几个二级坡、哪里补给、适合什么强度」
   - 价值：把路书从「数据」升级为「建议」

3. **历史骑行一键转路书** ✅ 已交付
   - 把某次骑得爽的活动记录转存为路书，可反复复用
   - 价值：路书和数据天然耦合

### ✅ 阶段三（已完成）：路径规划

> 原则：**不自研路由引擎**，接第三方（高德），只做「途经点 + 规划」层。

1. **接高德骑行 API** ✅ 已交付
   - 高德地图交互选点（行者风格）：点击地图添加途经点，相邻点之间由高德逐段规划骑行路线
   - 坐标纠偏：高德 GCJ-02 → WGS-84，导出路书不漂移
   - 前置：需高德「Web服务」类型 Key（与轨迹页「JS API」Key 是不同类型）

2. **AI 规划层**（差异化价值）
   - 用户说「周末想骑 80km，中等强度，有水库、终点能补给」
   - → AI 拆解成约束（里程/爬升/POI/补给）
   - → 调路由引擎出候选 → AI 对比点评推荐 → 生成路书

3. **自建 OSRM**（长期可选，2核2G 可跑）
   - 中国路网数据约几个 GB，完全离线
   - 适合长期自用，但部署稍重，非必需

### 🧭 阶段三.5：输入内容自动规划生成路书（新方向）

> **一句话输入 → 自动生成完整路书**，适合**长距离骑行 / 跨市 / 跨省骑行**（短途点击选点足够，长途手动打点太累、且高德分段规划对超长距离/跨城支持有限）。

1. **输入自然语言需求** → 自动规划
   - 例：「从北京到天津，走国道，绕城郊风景好的路，总里程 120~150km，中等强度，沿途有补给，终点能坐高铁回来」
   - AI 拆解：起点城市 / 终点城市 / 途经地标 / 里程目标 / 强度偏好 / 补给点 / 返程方式

2. **多段串联 + 跨城路由**
   - 长距离拆成若干 20~50km 的「分站」（每站一个落脚点/补给点），逐段调用高德骑行规划
   - 跨市/跨省时衔接长途国道（G 字头），必要时混合驾车/骑行路线分段取「实际可骑段」
   - 结果拼成完整路书：里程、分段爬升、每段强度评估、补给点标注

3. **生成结构化路书 + 建议**
   - 输出：总里程/累计爬升/预计用时/各段概览/AI 点评（强度是否匹配、补给是否够、天气提醒）
   - 可导出 GPX + 转存为路书反复复用

> 价值：把路径规划从「手动打点」升级为「说出需求直接出方案」，是长途骑行的核心场景。

---

## 三、其它可做的方向（按需）

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 输入内容自动规划路书 | 一句话输入（如「北京到天津 120km 国道 有补给」）自动生成完整路书，适合长距离/跨市/跨省骑行 | 高 |
| 训练负荷/恢复建议 | 已完成基础版，可继续打磨 | 中 |
| 训练后恢复建议 🧩 | 按心率强度/时长/温度/体重，输出拉伸冷身、补水、蛋白质/碳水、睡眠、主动恢复建议（参考其口径，非医疗建议） | 中 |
| 3D 路线景观 🧩 | 坡度着色立体路线 + 海拔幕帘 + 骑手光点动画 + 导出 MP4（参考其 Three.js 思路，对 Python 桌面版成本高，作远期亮点） | 低 |
| 装备维护管家 | 里程驱动保养提醒 + 故障问答 | 中 |
| 骑行安全 agent | 摔车检测、天气/路况提醒 | 低 |
| 营养补给计划 | 按里程+强度算补水量/电解质/碳水 | 低 |

---

## 四、待办 / 已知问题

- [ ] FTP 字段需手动填（`ftp_w`），后续可做自动估算或更友好配置
- [ ] 佳明路书文件（`COURSE_*.fit`）无码表型号/最大速度，README 已说明，属数据限制
- [ ] GUI 打包依赖 PyInstaller + WebEngine，本机需设 `CODEBUDDY_SAFE_DELETE_ENABLED=0` 绕过 safe-delete 守卫（已记入编辑器记忆）
- [ ] 🧩 序列降采样策略：FIT 点密集时压缩到上限点数（如 2000），统计峰值在压缩前用全量数据算，保峰值不丢（参考其做法）

---

## 五、近期可立即动手的一条建议

**首推「输入内容自动规划路书」**：路径规划的「地图点击选点」已跑通（阶段三），在此基础上把「AI 规划层」落地——用户一句话输入需求（里程/强度/补给/跨城），AI 拆解成途经点序列 + 分段规划 + 生成完整路书。复用已有的 LLM、高德骑行规划、路书分析三块能力，是为长途骑行补齐的最后一块核心拼图。

---

## 六、参考来源（2026-09-02 增补）

| 来源 | 说明 | 借鉴边界 |
|------|------|----------|
| `hanfengown/fit-ride-analytics`（Gitee） | 纯前端 FIT 分析 SPA（Deepseek Harness 制作，AGPL-3.0） | 仅借鉴**算法口径/呈现思路**，不抄其代码（尤其 `route-scene.js` 3D 部分）；直接用须注意 AGPL 传染 |
| 本工具包 | fit-file-parser / Chart.js / Leaflet / Three.js / mp4-muxer | 技术选型参考，Python 桌面版对应替代：fitparse / matplotlib/PyQt / 高德 |

